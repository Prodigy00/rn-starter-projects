import React from "react";
import { View, Text, StyleSheet } from "react-native";

const ImageDetail = ({ imageName }) => {
  return <Text>{imageName}</Text>;
};

const styles = StyleSheet.create({});

export default ImageDetail;
